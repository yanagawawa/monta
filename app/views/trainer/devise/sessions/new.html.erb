<div class="text-center my-3">
<h2>トレーナーログイン</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <table class="row justify-content-md-center">
        <tbody>
          <tr>
            <td class="form-floating">
              <%= f.label "メールアドレス" %><br />
              <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "name@example.com", class: "form-control" %>
            </td>
          </tr>
          <tr>
            <td class="form-floating">
              <%= f.label "パスワード" %><br />
              <%= f.password_field :password, autocomplete: "current-password", placeholder: "Password", class: "form-control",  size: "1x10" %>
            </td>
          </tr>
          <tr>
            <% if devise_mapping.rememberable? %>
              <td class="field">
                <%= f.check_box :remember_me %>
                <%= f.label :remember_me %>
              </td>
            <% end %>
          </tr>
          <tr>
            <td class="actions">
              <%= f.submit "ログイン", class: "btn btn-lg btn-primary btn-block" %>
            </td>
          </tr>
        </tbody>
      </table>
    <% end %>
    <%= render "devise/shared/links" %>
</div>